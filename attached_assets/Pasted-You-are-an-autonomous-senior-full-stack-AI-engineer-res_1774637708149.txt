You are an autonomous senior full-stack AI engineer responsible for fixing, completing, and stabilizing the Dashboard of InvoiceFlow.

Immediately audit and repair the entire Dashboard system to make it fully functional, data-driven, and production-ready.

--------------------------------------------------

1. DATA INTEGRATION

- Replace all placeholder values (₦0.00) with real data.
- Connect dashboard components to backend APIs.
- Implement endpoints if missing:
  - GET /dashboard/summary
  - GET /invoices/recent
  - GET /payments/recent
  - GET /analytics/revenue-expenses

- Ensure accurate calculations for:
  revenue, outstanding invoices, overdue invoices, expenses, net profit.

--------------------------------------------------

2. METRICS CARDS FIX

- Ensure each metric card displays real values.
- Add:
  - loading skeleton states
  - empty states (no data)
  - error states with retry

- Apply proper visual emphasis:
  - Revenue (primary)
  - Profit (green)
  - Overdue (red)
  - Outstanding (warning)

--------------------------------------------------

3. CHARTS REPAIR

- Fix “Revenue vs Expenses” chart:
  - Ensure it renders real data
  - Add loading and empty states
  - Validate chart configuration

- Replace “Invoice Status” with a proper breakdown:
  - Draft, Sent, Paid, Overdue
  - Use a donut/pie chart

--------------------------------------------------

4. RECENT DATA MODULES

- Fix “Recent Invoices”:
  - Fetch real invoice data
  - Display table with key fields
  - Improve empty state with onboarding CTA

- Fix “Recent Payments”:
  - Fetch and display real payments
  - Add “Record Payment” CTA if empty

--------------------------------------------------

5. QUICK ACTIONS

- Ensure all buttons work correctly:
  - Create Invoice → /invoices/new
  - New Estimate → /estimates/new
  - Add Client → /clients/new

- Verify forms open and submit successfully.

--------------------------------------------------

6. GLOBAL SEARCH

- Implement functional search:
  - Search invoices, clients, expenses
  - Add debounce
  - Show dropdown results
  - Enable keyboard navigation

--------------------------------------------------

7. NOTIFICATIONS PANEL

- Fix UI positioning and alignment.
- Connect to real notifications data.
- Add:
  - unread indicators
  - timestamps
  - actions

--------------------------------------------------

8. UI/UX IMPROVEMENTS

- Improve visual hierarchy and spacing.
- Reduce empty space and increase content density.
- Add:
  - activity feed (recent actions)
  - actionable insights (e.g. overdue invoices alert)

- Ensure full responsiveness across desktop, tablet, and mobile.

--------------------------------------------------

9. STATE HANDLING

- Implement:
  - loading states (skeletons)
  - empty states
  - error handling with retry

- Ensure UI always reflects latest server data.

--------------------------------------------------

10. PERFORMANCE

- Optimize data fetching (React Query or equivalent).
- Add caching and pagination where needed.
- Prevent unnecessary re-renders.

--------------------------------------------------

11. TESTING

- Add tests for:
  - dashboard data loading
  - chart rendering
  - quick actions
  - search functionality

--------------------------------------------------

12. FINAL VALIDATION

Ensure:

- all metrics display real data
- charts render correctly
- all buttons work
- no empty or broken sections
- no console errors
- dashboard feels like a live SaaS product

--------------------------------------------------

FINAL OBJECTIVE

Deliver a fully functional, interactive, data-driven, polished, and production-ready Dashboard with zero broken features, ready for real-world users.

Execute immediately and do not stop until the Dashboard is fully stable and complete.