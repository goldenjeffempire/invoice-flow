You are an autonomous elite Senior Full-Stack + Security + DevOps + UI/UX engineer. Fully audit and fix InvoiceFlow end-to-end using BOTH the live app https://invoiceflow.com.ng and the full repo https://github.com/goldenjeffempire/invoice-flow. Perform a true page-by-page live audit and file-by-file/line-by-line repo audit across templates/components, backend views/controllers, services, models, DB schema/migrations, static/assets/build pipeline, APIs, auth/session, payments/webhooks (Paystack/Stripe if present), background jobs/reminders, configs, deployment, and tests. Identify and eliminate ALL bugs/errors/warnings, 500s, broken routes, dead links/CTAs, missing/incomplete pages, placeholder logic (pass/…/TODO/FIXME), misconfigurations, performance bottlenecks, and security gaps. Enforce strict layer mapping (models→services→views/APIs→templates/components), unify validation rules (server authoritative; client mirrors), enforce object-level permissions everywhere, and standardize error handling UX + API envelopes (consistent status codes, inline field errors, toasts, empty/loading/success/error states). Fix production-grade security: CSRF, secure cookies, session rotation/expiry, rate limiting for auth, password policy, email verification + reset token expiry, secure headers/CSP, audit logging; remove any hardcoded secrets and move to env with .env.example + config validation. Harden payments: verify webhook signatures, implement idempotency/replay protection, transactional invoice/payment updates, duplicate event handling, and clear user-visible statuses. Optimize performance: eliminate N+1 queries, add indexes, cache safe aggregates, optimize static assets (minify/compress/fingerprint/cache headers), reduce blocking scripts, improve Lighthouse/core web vitals. Modernize UI/UX across all public and authenticated pages with a unified design system (tokens, reusable components, responsive/mobile-first, accessibility/WCAG, keyboard/focus states, micro-animations, skeletons). Add/repair comprehensive tests (unit+integration+security+webhook) and CI to run lint/tests; ensure clean installs and prod deployments. Output (1) a structured issues report with severity and exact file/route references, (2) PR-ready commits grouped by phase, (3) proof via tests/logs that all critical flows work: signup/login/logout, verification/reset, invoice CRUD + send, payments + webhook updates, reminders/automation, dashboards/analytics, and (4) updated deployment docs; final state must have ZERO broken flows, ZERO dead links, ZERO placeholders, and be production-ready.
