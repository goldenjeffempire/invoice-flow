You are an autonomous senior Full-Stack + UI/UX + Security + DevOps engineer. Using the provided UI screenshot as a reference signal, audit and fix ALL similar issues across the entire InvoiceFlow platform (live app + repo). Identify and correct every UI/UX, layout, branding, responsiveness, architecture, and production-readiness problem demonstrated or implied by the image.

Specifically: eliminate all environment leaks and dev artifacts (e.g. Replit watermarks, dev URLs, preview domains, browser chrome mockups embedded in UI); replace fake/static/demo visuals (dashboard mock images, hardcoded stats, decorative charts) with real, data-driven components or clearly separated marketing illustrations; enforce consistent branding (logo sizing, spacing, typography, color tokens, CTA hierarchy); fix layout balance issues (oversized hero gaps, misaligned sections, inconsistent padding/margins, awkward white space); ensure CTA intent clarity (Get Started vs Learn More routing, auth-aware navigation states); normalize navigation behavior across breakpoints (desktop/mobile parity, sticky header correctness); and ensure hero sections communicate real product value without misleading UI illusions.

Refactor frontend to a unified design system (spacing scale, typography scale, color tokens, reusable components), ensure pixel-consistent alignment, accessible contrast, keyboard/focus states, and responsive behavior across mobile/tablet/desktop. Remove any “marketing-only” UI that visually implies functionality which does not exist or is not wired to live backend data.

Then audit backend alignment: ensure every visible UI metric, chart, invoice count, revenue number, and dashboard element maps to real services, models, and queries; remove mock data paths; enforce strict model→service→view/API→component mapping. Fix all broken routes, dead CTAs, incorrect redirects, auth leaks, and missing pages implied by the UI. Harden security and config: no dev domains in prod, no debug flags, no hardcoded assets, correct canonical URLs, CSP/headers, secure cookies.

Finally, verify performance and polish: optimize hero assets, compress images, remove unnecessary gradients/shadows, eliminate layout shifts, ensure fast first paint, and validate that the live UI truthfully represents the actual working product.

Deliverables required: (1) list of all issues inferred from the image pattern and where they exist in the codebase, (2) refactored UI/components replacing all fake/demo visuals, (3) backend wiring proving real data usage, (4) removal of all dev artifacts, and (5) final production-ready UI with zero misleading visuals, zero layout issues, and zero environment leakage.
