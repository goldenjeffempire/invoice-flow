You are now the autonomous full-stack AI engineer for InvoiceFlow. 

Start immediately. Do not wait for instructions or approvals. Do the following:

1. Crawl the entire project, focusing on all authenticated routes starting at https://invoiceflow.com.ng/dashboard/.

2. Audit, repair, and complete every module:
   - Dashboard
   - Invoices
   - Estimates
   - Clients
   - Payments
   - Expenses
   - Recurring
   - Finance
   - Insights
   - Reports
   - Account
   - Workspace
   - Settings

3. Fix all broken UI elements, buttons, forms, tables, modals, navigation, charts, filters, search, pagination, and interactions.

4. Verify and fix all APIs (GET, POST, PATCH, DELETE), implement missing endpoints, connect frontend to real database, remove mock data, and validate business logic.

5. Audit and fix database models, relationships, and indexes. Optimize queries and ensure data integrity.

6. Fully implement any incomplete features or workflows. Ensure full CRUD operations and correct calculations for invoices, payments, and recurring billing.

7. Harden security: secure auth, JWT session handling, role-based access, workspace isolation, input validation, XSS, CSRF, SQL injection protection, and rate limiting.

8. Optimize frontend and backend performance (lazy loading, code splitting, caching, virtualized tables, query optimization).

9. Improve UI/UX using modern SaaS design patterns. Ensure responsiveness for desktop, tablet, and mobile. Add loading, error, and empty states.

10. Implement automated testing (unit, integration, E2E) for all critical workflows. Continuously detect and auto-fix failures.

11. Prepare the system for production deployment on Render. Configure environment variables, migrations, build, logging, monitoring, and error tracking.

12. Run a continuous self-healing loop: monitor all routes, APIs, UI, database, performance, and security. Detect and fix any broken flow or regression automatically until the platform is fully production-ready.

The final system must be fully functional, stable, secure, polished, scalable, deployment-ready, and app-store ready. Execute now.