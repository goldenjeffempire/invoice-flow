Act as an autonomous “God-Mode” full-stack AI engineer and immediately take control of the entire InvoiceFlow platform.

Audit, rebuild, restructure, enhance, validate, and stabilize all pages and systems (public and authenticated), including Landing, Features, Pricing, Auth (Login/Register/Reset), Dashboard, Invoices, Estimates, Clients, Payments, Expenses, Recurring, Finance, Insights, Reports, Account, Workspace, and Settings.

Ensure every feature and functionality is fully operational and production-ready. Verify that every button, form, input, modal, table, filter, search, and navigation element works correctly, triggers the right actions, connects to real APIs, and completes all workflows end-to-end without failure.

Fix all issues including broken UI, incomplete features, placeholder data, API failures, missing endpoints, incorrect logic, database inconsistencies, and performance bottlenecks. Fully implement all missing functionalities, including complete invoice workflows, client management, payments, recurring billing, analytics, and reporting with accurate business logic and real data integration.

Standardize and enhance the UI/UX using modern SaaS design patterns with responsive layouts, intuitive navigation, consistent design, and proper loading, empty, and error states.

Enforce production-grade security with authentication, JWT session handling, RBAC, workspace isolation, input validation, and protection against XSS, CSRF, SQL injection, and abuse.

Optimize performance across frontend and backend using lazy loading, code splitting, caching, indexed queries, pagination, and efficient data fetching.

Continuously test and validate all flows (unit, integration, end-to-end), detect and fix regressions, and maintain data consistency with no stale or broken states.

Run a continuous self-healing loop that monitors all pages, APIs, workflows, and system performance, automatically fixing issues until zero bugs, zero broken features, and full functional integrity are achieved.

Prepare the system for production deployment with proper environment configuration, database migrations, build optimization, logging, monitoring, and error tracking, ensuring readiness for Render and web/mobile deployment.

Do not stop until the entire platform is fully functional, stable, secure, scalable, polished, and production-ready for real-world users.