Implement all missing and incomplete backend + frontend code and files end-to-end so the platform becomes fully enterprise-grade, fully corresponding across layers, fully operational, and production-ready for real-world users: audit the entire repo for missing templates, broken routes, stubbed services, placeholder logic (pass, ..., TODOs), inconsistent models/serializers/views, incomplete payment/email/reminder flows, and any TemplateDoesNotExist/404/500 paths; then complete or refactor each area so every UI page maps cleanly to real backend functionality (models → services → views/APIs → templates/components) with strict validation, permissions, and consistent error handling. Ensure all core workflows work end-to-end: auth (signup/login/verify/reset/session), dashboard, invoices (create/edit/send/view/pdf/status), clients, payments (Paystack/Stripe init + callbacks + webhooks + idempotency + reconciliation), reminders/automations (scheduled sending + toggles + audit logs), settings (profile, org, email, payment config), admin, and public pages—with Pricing fully removed everywhere (routes, templates, links, data, copy). Implement complete UX states across the frontend (loading/skeleton, empty, error, success, toasts), consistent enterprise UI system (grid/spacing/typography/tokens/components), full responsiveness across all device sizes (320px → ultrawide) with no layout breakage/CLS, accessibility (WCAG: semantic structure, keyboard nav, focus states, contrast, reduced-motion), and performance optimizations (lazy-load, compressed responsive assets, caching). Harden backend for production (secure env validation, strict ALLOWED_HOSTS/CSRF, rate-limiting where needed, secure webhook verification, logging/monitoring, health checks, migrations/static handling), ensure Render deployment readiness (gunicorn, collectstatic, DB migrations, env vars), and verify completion by running a full route crawl + integration tests confirming no missing files, no dead links/CTAs, no broken pages, and all features function correctly end-to-end in production.